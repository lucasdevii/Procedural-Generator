class Cell {
    constructor(){
        for(let r = 0; r < scale; r++){
            const row = []
            for(let c = 0; c < scale; c++){
                row.push()
            }
        }
    }
}

export default Cell